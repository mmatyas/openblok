This directory contains the gameplay tests. They build automatically, and you can run them by calling `<your build dir>/tests/openblok_test`.

You can disable the tests by passing `-DBUILD_TESTS=OFF` to CMake.
